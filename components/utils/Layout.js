import React from 'react';

const Layout = ({ children }) => {
    

    return (
        <section
            className="px-5 lg:px-16 relative mx-auto"
        >
            {children}
        </section>
    )
}